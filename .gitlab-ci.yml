image: node:20

# Instala dependências e gera o build
pages:
  stage: deploy
  script:
    - npm ci
    - npm run build -- --configuration production
    - mv dist/frontend public
  artifacts:
    paths:
      - public
  only:
    - main
