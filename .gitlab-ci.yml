image: node:20-alpine

stages:
  - deploy

pages:
  stage: deploy
  script:
    - npm install
    - node scripts/generate-keys.js   # gera keys.js no CI/CD
    - ls -la public/js                # só pra debug
  artifacts:
    paths:
      - public
  only:
    - master
