image: alpine:latest

pages:
  stage: deploy
  script:
    - echo "Deploying WS-Gestão to GitLab Pages..."
    - cp keys.ci.js public/js/keys.local.js
    - echo "Sem keys CI, apenas login Google"
    - ls -la public
  artifacts:
    paths:
      - public
  only:
    - master
